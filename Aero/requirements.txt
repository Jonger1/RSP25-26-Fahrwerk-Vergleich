# Was Aero Studio zum Laufen braucht.
#
# Diese Datei ist die einzige Wahrheit. "Aero Studio.bat" installiert aus ihr,
# damit Starter und Paketliste nicht auseinanderlaufen koennen - vorher stand
# die Liste nur in der .bat, und matplotlib fehlte dort seit jeher (siehe
# unten).
#
# Untergrenzen statt fester Versionen: Das Werkzeug wird ueber Jahre von
# wechselnden Teammitgliedern auf wechselnden Rechnern gestartet. Feste
# Versionen waeren nach zwei Semestern nicht mehr baubar, ohne dass es jemand
# merkt. Eine Untergrenze steht nur dort, wo wir eine Programmierschnittstelle
# benutzen, die es vorher nicht gab - sie ist damit begruendbar und nicht
# geraten.
#
# Nachinstallieren von Hand:   .venv\Scripts\python -m pip install -r requirements.txt

# --- Rechnen ---------------------------------------------------------------
numpy>=1.24
scipy>=1.10             # PCHIP fuer die Spannweitenverteilungen, Optimierung

# --- Datenmodell und Konfiguration -----------------------------------------
pydantic>=2.0           # ConfigDict und model_validator gibt es erst ab 2.0;
                        # mit pydantic 1 startet das Spec-Modell gar nicht
PyYAML>=6.0             # Spec, Reglement und Fahrzeugbezug sind YAML

# --- Oberflaeche -----------------------------------------------------------
dash>=2.9               # ab hier heisst der Start app.run statt run_server
plotly>=5.15

# --- Geometrie -------------------------------------------------------------
matplotlib>=3.5
# Nur wegen matplotlib.path.Path.contains_points in
# geometrie/kaskade.py:schneiden_sich - der Kollisionspruefung zwischen zwei
# Elementen einer Kaskade. Das ist wenig Nutzen fuer ein so grosses Paket,
# aber es steht hier, weil es sonst nur als Beifang von neuralfoil
# hereinkommt (neuralfoil -> aerosandbox -> matplotlib). Die Profilpolare
# faengt ein fehlendes neuralfoil bewusst ab und arbeitet ohne weiter - dann
# faellt die Kaskadenpruefung mit einem nackten ImportError um, und niemand
# sieht den Zusammenhang. Entweder es steht hier, oder schneiden_sich rechnet
# den Punkt-im-Polygon selbst.

# --- Aerodynamik -----------------------------------------------------------
neuralfoil>=0.3
# Zieht AeroSandbox samt pandas, seaborn und casadi nach und ist damit der
# mit Abstand groesste Brocken der Installation. aero/profilpolare.py kommt
# ohne es aus und sagt dann verstaendlich, was fehlt - wer nur Geometrie und
# Export braucht, kann die Zeile also streichen.

# Nicht mehr dabei: ezdxf und shapely. Beide standen in der .bat, werden aber
# von keiner einzigen Zeile importiert - ezdxf war fuer den DXF-Writer (M1
# Punkt 9) vorgesehen, der noch nicht existiert, shapely fuer die
# Kollisionspruefung (M2 Punkt 3), die am Ende ohne es gebaut wurde. Sie
# kommen zurueck, sobald sie jemand wirklich benutzt.
